# Rvbee Monitor Configuration (1 monitor)
# This file contains monitor-specific settings for rvbee

# Monitor configuration for single monitor setup
monitor=,preferred,auto,auto

# Single monitor specific settings
# No workspace assignments needed for single monitor
# All workspaces will be available on the single monitor
